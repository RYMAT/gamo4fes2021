import { ScrollStyleDirective } from './scroll-style.directive';

describe('ScrollStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollStyleDirective();
    expect(directive).toBeTruthy();
  });
});
